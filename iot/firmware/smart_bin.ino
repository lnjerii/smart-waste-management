// ESP32 pseudo-firmware starter
// Replace with actual sensor wiring and GSM/WiFi transport stack.

void setup() {
  Serial.begin(115200);
}

void loop() {
  // Read ultrasonic distance and convert to fill level
  // Read temperature sensor
  // Build JSON payload and POST to /api/v1/telemetry
  delay(600000); // 10 minutes
}
